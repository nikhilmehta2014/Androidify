<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nikhil.androidify">

    <application
        android:name=".AndroidifyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity
            android:name=".launchmodes.activity.FirstActivity"
            android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".launchmodes.activity.SecondActivity"
            android:launchMode="standard" />
        <activity
            android:name=".launchmodes.activity.ThirdActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".launchmodes.activity.FourthActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".launchmodes.activity.FifthActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".launchmodes.activity.SixthActivity"
            android:launchMode="singleInstance" />
    </application>

</manifest>